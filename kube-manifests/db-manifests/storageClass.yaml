apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata: 
  name: ebs-sc
provisioner: ebs.csi.aws.com # if your using EBS then use this provisioner otherwise, you can go on your own.
volumeBindingMode: WaitForFirstConsumer 